# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = [
  "cd Backend",
  "pip install -r requirements.txt"
]

[start]
cmd = "cd Backend && uvicorn routes:app --host 0.0.0.0 --port $PORT"
